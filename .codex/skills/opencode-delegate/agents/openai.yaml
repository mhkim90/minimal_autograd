interface:
  display_name: "OpenCode Delegate"
  short_description: "Route work through OpenCode MCP agents"
  default_prompt: "Use $opencode-delegate to route a focused task through the OpenCode MCP tools."
