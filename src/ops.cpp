// src/ops.cpp — placeholder.
//
// All op free functions are defined `inline` in include/autograd/ops.h
// so they are available wherever the header is included. This translation
// unit is kept for the build system to compile a single source for
// autograd_static (cmake adds it to the lib).
#include "autograd/ops.h"
